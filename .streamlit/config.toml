[theme]
primaryColor="#32CD32"  # Lime Green (bolder)
backgroundColor="#1A232F" # Slightly darker background
secondaryBackgroundColor="#37474F" # Darker blue-grey
textColor="#FFFFFF" # White text for contrast
font="sans serif"

[server]
# You can add server configurations here if needed,
# but for basic theming, the [theme] section is enough.
# headless = true
# port = 8501
# enableCORS = false